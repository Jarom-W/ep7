[Unit]
Description=Deploy Ready Together from main

[Service]
Type=oneshot
EnvironmentFile=%h/.config/ready-together-deploy/environment
ExecStart=%h/.local/lib/ready-together/poll-and-deploy.sh ${REPO_DIR} ${STATE_DIR} ${COMPOSE_PROJECT}
TimeoutStartSec=30min
UMask=0077
